# دستورات مفید
- `pnpm dev` — اجرای سرور توسعه (Hot Reload)
- `pnpm build` — ساخت برنامه برای تولید
- `pnpm start` — اجرای خروجی تولیدی
- `pnpm lint` — اجرای linters (در صورت وجود)

اگر از npm استفاده می‌کنید: `npm install`، `npm run dev` و... را متناظر اجرا کنید.

## ساختار پوشه‌ها (خلاصه)

- `src/app/` — صفحات اپلیکیشن (روت‌ها و لایه‌ها)
- `src/components/` — کامپوننت‌های عمومی و UI
- `src/features/` — بخش‌های منطقی (auth, products, navbar, discover)
- `src/lib/` — کمک‌افزارها و توابع کمکی
- `public/` — فایل‌های عمومی مثل تصاویر و service worker

برای مشاهده ساختار دقیق، به پوشه `src` نگاه کنید.

## نکاتی درباره توسعه
- این پروژه با TypeScript ساخته شده — تایپ‌ها را رعایت کنید.
- از استانداردهای Tailwind برای استایل‌ها استفاده شده است.
- فایل `sw.js` در `public/` نمونه‌ای از service worker است؛ هنگام توسعه PWA به آن توجه کنید.

## متغیرهای محیطی
اگر پروژه به احراز هویت یا API نیاز داشته باشد، متغیرهای مربوط را در فایل `.env` قرار دهید (مثلاً مقادیر مربوط به NextAuth یا کلیدهای سرویس‌ها). فایل نمونه `.env.example` را (در صورت وجود) بررسی کنید.

## مشارکت
- اگر مایل به مشارکت هستید، یک issue باز کنید یا یک Pull Request ارسال نمایید.
- لطفاً قبل از ارسال PR قوانین فرمت کد و تست‌های موجود را رعایت کنید.

## لایسنس
این پروژه تحت لایسنس موجود در فایل `LICENSE` قرار دارد.

---

اگر بخواهید، می‌توانم README را با جزئیات بیشتر (مثلاً نمونه مسیرهای API، دستورالعمل‌های توسعه‌دهنده، یا تصاویر اسکرین‌شات) بسط دهم — به من بگویید چه بخش‌هایی را می‌خواهید اضافه کنم.
